import styled from "styled-components";

export const Scroller = styled.ol`
  overflow-x: scroll;
  overflow-y: hidden;
  display: flex;
  list-style-type: none;
  ${
    "" /* margin-left: -10px;
  margin-top: -10px; */
  }
`;
